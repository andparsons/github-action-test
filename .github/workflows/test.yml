name: Test

on:
  push:
    branches: [ main ]


jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
        with:
          clean: false
          persist-credentials: false
      - uses: spyoungtech/mirror-action@master
        with:
          REMOTE: "ssh://beespoke@185.216.76.236:2020/home/beespoke/.repo"
          GIT_USERNAME: beespoke
          GIT_SSH_PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          GIT_SSH_PUBLIC_KEY: ${{ secrets.PUBLIC_KEY }}
          GIT_SSH_NO_VERIFY_HOST: "true"